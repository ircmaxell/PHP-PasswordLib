<?xml version="1.0" encoding="UTF-8"?>
<project name="PHP-CryptLib" default="" basedir=".">
    <target name="clean">
        <delete dir="build" />
        <delete dir="docs/api" />
    </target>
    <target name="prepare">
        <mkdir dir="build/results" />
        <mkdir dir="docs/api" />
    </target>
    <target name="cs" depends="prepare">
        <exec
            command="phpcs --report=summary --report-checkstyle=./build/results/checkstyle.xml --standard=test/CS/ruleset.xml lib"
            os="Linux" 
            passthru="true"
            checkreturn="true" />
        <exec
            command="phpcs.bat --standard=test/CS/ruleset.xml lib"
            os="WIN" checkreturn="true" />
    </target>
    <target name="doc">
        <exec command="phpdoctor.php phpdoctor.ini" />
    </target>
</project>
