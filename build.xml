<?xml version="1.0" encoding="UTF-8"?>
<project name="PHP-CryptLib" default="" basedir=".">
    <property name="libdir" value="${project.basedir}/lib" override="true" />
    <property name="builddir" value="${project.basedir}/build" override="true" />
    <property name="resultsdir" value="${project.basedir}/build/results" override="true" />

    <target name="clean">
        <delete dir="build" />
        <delete dir="docs/api" />
    </target>
    <target name="prepare">
        <mkdir dir="build/results" />
        <mkdir dir="docs/api" />
    </target>
    <target name="cs" depends="prepare">
        <exec
            command="phpcs --report=summary --report-checkstyle=${resultsdir}/checkstyle.xml --standard=test/CS/ruleset.xml lib"
            os="Linux" 
            passthru="true"
            checkreturn="true" />
        <exec
            command="phpcs.bat  --report=summary --report-checkstyle=${resultsdir}/checkstyle.xml --standard=test/CS/ruleset.xml lib"
            os="WIN" checkreturn="true" />
    </target>

</project>
